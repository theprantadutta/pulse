org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Every plugin in use is AndroidX-native, so Jetifier is dead work.
android.enableJetifier=false
android.newDsl=false
android.builtInKotlin=false
android.defaults.buildfeatures.resvalues=true
android.sdk.defaultTargetSdkToCompileSdkIfUnset=false
android.enableAppCompileTimeRClass=false
android.usesSdkInManifest.disallowed=false
android.uniquePackageNames=false
android.dependency.useConstraints=true
android.r8.strictFullModeForKeepRules=false
android.r8.optimizedResourceShrinking=false
# The project lives on G:\ but the Pub cache lives on C:\. Kotlin's incremental
# compiler cannot relativize paths across drive letters and crashes, so use
# full (non-incremental) compilation.
kotlin.incremental=false
